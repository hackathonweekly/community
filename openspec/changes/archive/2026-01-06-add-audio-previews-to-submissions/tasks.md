## 1. 实现
- [x] 识别作品的首个音频附件（`fileType === "audio"`），并在「作品详情」与「投屏模式」中可用。
- [x] 更新 `SubmissionDetail`：当存在音频附件时渲染 `<audio controls preload="metadata">` 播放器。
- [x] 确保 `coverImage` + 音频渲染为「封面图 + 音频播放器」（不开启自动播放）；保持现有视频预览行为不变。
- [x] 更新 `SubmissionsSlideDeck`：为当前作品渲染同样的音频预览行为。
- [ ] （可选）优化附件列表：对音频附件提供内嵌播放器（而不是仅外链下载），且不改变非音频附件行为。

## 2. 验证
- [ ] 手动：打开仅包含音频附件的作品，确认可正常播放。
- [ ] 手动：打开包含 `coverImage` + 音频的作品，确认两者同时可见且音频可播放。
- [ ] 手动：打开包含视频附件的作品，确认视频预览仍正常。
- [x] 运行 `pnpm lint`。
- [ ] 运行 `pnpm type-check`（当前仓库已有类型错误导致失败，非本次变更引入）。
